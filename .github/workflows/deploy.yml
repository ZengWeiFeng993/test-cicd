name: Deploy test-cicd
on: [ push ]
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{secrets.SSH_HOST}}
          username: ${{secrets.username}}
          password: ${{secrets.password}}
          script_stop: true
          script: |
            ls -al
            touch test.txt
            echo "hello gama1" >> test.txt
            cat test.txt


